MVP 1

**1. 인증·계정·질문·답변·멘토신청**

| **API/기능** | **GUEST** | **USER** | **MENTOR** | **ADMIN** |
| --- | --- | --- | --- | --- |
| F-01. 회원가입 (`POST /api/auth/signup`) | O | O | O | O |
| F-01-1. 로그인 (`POST /api/auth/login`) | O | O | O | O |
| F-01-2. 토큰 재발급 (`POST /api/auth/refresh`) | O(refreshToken 쿠키 필요) | O | O | O |
| F-02. 로그아웃 (`POST /api/auth/logout`) | 401 | O | O | O |
| F-03. 내 프로필 조회 (`GET /api/users/me`) | 401 | O(본인) | O(본인) | O(본인) |
| F-03-1. 프로필(이름·관심사) 수정 (`PATCH /api/users/me`) | 401 | O(본인) | O(본인) | O(본인) |
| F-03-2. 이메일 수정 (`PATCH /api/users/me/email`) | 401 | O(본인) | O(본인) | O(본인) |
| F-03-3. 비밀번호 수정 (`PATCH /api/users/me/password`) | 401 | O(본인) | O(본인) | O(본인) |
| F-04. 회원 탈퇴 (`DELETE /api/users/me`) | 401 | O(본인) | O(본인) | O(본인) |
| F-05. 질문 작성 (`POST /api/questions`) | 401 | O | O | O |
| F-06. 질문 목록 조회 (`GET /api/questions`) | O | O | O | O |
| F-07. 질문 상세 조회 (`GET /api/questions/{id}`) | O | O | O | O |
| F-08. 질문 수정 (`PATCH /api/questions/{id}`) | 401 | O(본인만, ADMIN 우회 없음) | O(본인만) | O(본인만) |
| F-08-1. 질문 삭제 (`DELETE /api/questions/{id}`) | 401 | O(본인 및 ADMIN) | O(본인 및 ADMIN) | O(본인 및 ADMIN) |
| F-09. 답변 작성 (`POST /api/questions/{id}/answers`) | 401 | O | O | O |
| F-09-1. 답변 목록 조회 (`GET /api/questions/{id}/answers`) | O | O | O | O |
| F-10. 답변 수정 (`PATCH /api/answers/{id}`) | 401 | O(본인만, ADMIN 우회 없음) | O(본인만) | O(본인만) |
| F-10-1. 답변 삭제 (`DELETE /api/answers/{id}`) | 401 | O(본인 및 ADMIN) | O(본인 및 ADMIN) | O(본인 및 ADMIN) |
| F-11. 멘토 신청 (`POST /api/users/me/mentor/application`) | 401 | O(본인) | 400(이미 멘토) | 400(대상 아님) |
| F-12. 멘토 신청 본인 조회 (`GET /api/users/me/mentor/application`) | 401 | O(본인) | O(본인) | O(본인) |
| F-13. 멘토 신청 대기 목록 조회 (`GET /api/admin/mentors/applications`) | 401 | 403 | 403 | O |
| F-14. 멘토 신청 승인 (`PATCH /api/admin/mentors/{userId}/approval`) | 401 | 403 | 403 | O |
| F-14-1. 멘토 신청 거절 (`PATCH /api/admin/mentors/{userId}/rejection`) | 401 | 403 | 403 | O |

MVP 2

**회원(User)**

| **API/기능** | **GUEST** | **USER** | **MENTOR** | **ADMIN** |
| --- | --- | --- | --- | --- |
| F-15. 전체 회원 목록 (`GET /api/users`) | 401 | 403 | 403 | O |
| F-16. 공개 프로필 조회 (`GET /api/users/{userId}`) | O | O | O | O |
| F-17. 공개 프로필 수정 (`PATCH /api/users/me/public-profile`) | 401 | O(본인) | O(본인) | O(본인) |
| F-18. 프로필 이미지 수정 (`PATCH /api/users/me/profile-image`) | 401 | O(본인) | O(본인) | O(본인) |
| F-19. 팔로우/언팔로우 토글 (`POST /api/users/{userId}/follow`) | 401 | O(자기 자신 대상은 400) | O | O |
| F-20. 이메일 인증코드 발송 (`POST /api/users/me/email/verification-code`) | 401 | O(본인) | O(본인) | O(본인) |
| F-21. 이메일 인증코드 검증 (`POST /api/users/me/email/verify`) | 401 | O(본인) | O(본인) | O(본인) |

**질문(Question)**

| **API/기능** | **GUEST** | **USER** | **MENTOR** | **ADMIN** |
| --- | --- | --- | --- | --- |
| F-22. 질문 좋아요 토글 (`POST /api/questions/{id}/like`) | 401 | O | O | O |
| F-23. 팔로잉 피드 (`GET /api/questions/following`) | 401(컨트롤러 자체 가드) | O(본인) | O(본인) | O(본인) |
| F-24. 특정 유저 작성 질문 목록 (`GET /api/questions/user/{userId}`) | 401 | 403 | 403 | O |
| F-25. 특정 유저 답변한 질문 목록 (`GET /api/questions/user/{userId}/answered`) | 401 | 403 | 403 | O |

**멘토(Mentor)**

| **API/기능** | **GUEST** | **USER** | **MENTOR** | **ADMIN** |
| --- | --- | --- | --- | --- |
| F-26. 멘토 목록 조회 (`GET /api/mentors`) | 401 | O | O | O |
| F-27. 멘토 상세 조회 (`GET /api/mentors/{mentorId}`) | 401 | O | O | O |
| F-28. 멘토 프로필 수정 (`PUT /api/mentors/{mentorId}`) | 401 | O(본인) | O(본인) | O(본인) |
| F-29. 멘토 리뷰 목록 조회 (`GET /api/mentors/{mentorId}/reviews`) | 401 | O | O | O |
| F-30. 멘토 리뷰 작성 (`POST /api/mentors/{mentorId}/reviews`) | 401 | O(해당 멘토 구독 이력 필요, 없으면 403 / 본인 대상 400) | O(구독 이력 필요) | O(구독 이력 필요) |
| F-31. 멘토 리뷰 삭제 (`DELETE /api/mentors/{mentorId}/reviews/{reviewId}`) | 401 | O(본인만, ADMIN 우회 없음) | O(본인만) | O(본인만) |

**멘토 대시보드**

| **API/기능** | **GUEST** | **USER** | **MENTOR** | **ADMIN** |
| --- | --- | --- | --- | --- |
| F-32. 대시보드 — 요약/트렌드/평점분포/프로필완성도/최근글/최근리뷰/구독자/환불/결제내역 (`GET .../summary` 외 8개) | 401 | 403 | O(본인만, 항상 JWT 기준) | 403 |

#### **멘토 요금제(Plan)**

| **API/기능** | **GUEST** | **USER** | **MENTOR** | **ADMIN** |
| --- | --- | --- | --- | --- |
| F-33. 요금제 목록 조회 (`GET /api/v1/mentors/{mentorId}/plans`) | 401 | O | O | O |
| F-34. 요금제 단건 조회 (`GET .../plans/{planId}`) | 401 | O | O | O |
| F-35. 요금제 등록 (`POST .../plans`) | 401 | O(본인, 정산계좌 미등록 시 400) | O(본인, 정산계좌 필요) | O(본인, 정산계좌 필요) |
| F-36. 요금제 수정 (`PUT .../plans/{planId}`) | 401 | O(본인만) | O(본인만) | O(본인만) |
| F-37. 요금제 삭제/비활성화 (`DELETE .../plans/{planId}`) | 401 | O(본인만) | O(본인만) | O(본인만) |

**멘토 아티클(Post)**

| **API/기능** | **GUEST** | **USER** | **MENTOR** | **ADMIN** |
| --- | --- | --- | --- | --- |
| F-38. 아티클 작성 (`POST /api/v1/mentors/{mentorId}/posts`) | 401 | O(본인) | O(본인) | O(본인) |
| F-39. 아티클 목록 조회 (`GET .../posts`) | 401 | O(공개 글은 O, 구독자 전용 글은 잠금 표시) | O | O |
| F-40. 아티클 상세 조회 (`GET .../posts/{postId}`) | 401 | O(공개 글 O / 구독자 전용 글은 멘토 본인·구독자만, 아니면 403) | 동일 | 동일 |
| F-41. 아티클 수정 (`PUT .../posts/{postId}`) | 401 | O(본인만) | O(본인만) | O(본인만) |
| F-42. 아티클 삭제 (`DELETE .../posts/{postId}`) | 401 | O(본인만) | O(본인만) | O(본인만) |
| F-43. 아티클 좋아요 (`POST .../posts/{postId}/likes`) | 401 | O | O | O |
| F-44. 아티클 좋아요 취소 (`DELETE .../posts/{postId}/likes`) | 401 | O | O | O |

**멘토 아티클 댓글**

| **API/기능** | **GUEST** | **USER** | **MENTOR** | **ADMIN** |
| --- | --- | --- | --- | --- |
| F-45. 댓글 목록 조회 (`GET /api/v1/mentors/{mentorId}/posts/{postId}/comments`) | 401 | O | O | O |
| F-46. 댓글 작성 (`POST .../comments`) | 401 | O | O | O |
| F-47. 댓글 삭제 (`DELETE .../comments/{commentId}`) | 401 | O(본인만) | O(본인만) | O(본인만) |

**구독(Subscription)**

| **API/기능** | **GUEST** | **USER** | **MENTOR** | **ADMIN** |
| --- | --- | --- | --- | --- |
| F-48. 구독 신청 (`POST /api/v1/subscriptions?planid=`) | 401 | O(이미 활성 구독 있으면 409) | O | O |
| F-49. 구독 해지 예약 (`PATCH .../{subscriptionId}/cancel`) | 401 | O(본인만) | O(본인만) | O(본인만) |
| F-50. 구독 권한 확인 (`GET .../check?mentorId=`) | 401 | O | O | O |
| F-51. 내 구독 목록 (`GET .../me`) | 401 | O(본인) | O(본인) | O(본인) |
| F-52. 구독별 결제 이력 (`GET .../{subscriptionId}/payments`) | 401 | O(본인만) | O(본인만) | O(본인만) |

**결제(Payment) · 결제수단(PaymentMethod)**

| **API/기능** | **GUEST** | **USER** | **MENTOR** | **ADMIN** |
| --- | --- | --- | --- | --- |
| F-53. 결제 완료 확정 (`POST /api/v1/payments/{paymentId}/complete`) | 401 | O(본인) | O(본인) | O(본인) |
| F-54. 환불 요청 (`POST .../{paymentId}/cancellations`) | 401 | O(본인) | O(본인) | O(본인) |
| F-55. 내 환불 이력 (`GET .../cancellations/me`) | 401 | O(본인) | O(본인) | O(본인) |
| F-56. 빌링키 발급 준비 (`POST /api/payment-methods/prepare`) | 401 | O | O | O |
| F-57. 결제수단 등록 (`POST /api/payment-methods`) | 401 | O(본인) | O(본인) | O(본인) |
| F-58. 결제수단 목록 조회 (`GET /api/payment-methods`) | 401 | O(본인) | O(본인) | O(본인) |
| F-59. 기본 결제수단 지정 (`PATCH .../{id}/default`) | 401 | O(본인) | O(본인) | O(본인) |
| F-60. 결제수단 삭제 (`DELETE .../{id}`) | 401 | O(본인) | O(본인) | O(본인) |

**채팅(Chat) · 알림(Notification)**

| **API/기능** | **GUEST** | **USER** | **MENTOR** | **ADMIN** |
| --- | --- | --- | --- | --- |
| F-61. 채팅방 생성/조회 (`POST /api/v1/mentors/{mentorId}/chat-room`) | 401 | O(해당 멘토 구독 이력 필요, 없으면 403 / 본인 대상 400) | O(멘티 입장에서 동일 규칙 적용) | O |
| F-62. 내 채팅방 목록 (`GET /api/v1/chat-rooms/me`) | 401 | O(본인) | O(본인) | O(본인) |
| F-63. 메시지 전송 (`POST .../{roomId}/messages`) | 401 | O(참여자만, 구독 만료 시 403) | O(참여자만) | O(참여자만) |
| F-64. 메시지 목록 조회 (`GET .../{roomId}/messages`) | 401 | O(참여자만) | O(참여자만) | O(참여자만) |
| F-65. 채팅 종료 (`DELETE .../{roomId}`) | 401 | O(참여자만) | O(참여자만) | O(참여자만) |
| F-66. 알림 목록/안읽음 개수 (`GET /api/v1/notifications`, `/unread-count`) | 401 | O(본인) | O(본인) | O(본인) |
| F-67. 알림 읽음 처리 — 단건/전체 (`PATCH .../{id}/read`, `/read-all`) | 401 | O(단건은 본인만, 아니면 403) | 동일 | 동일 |
| F-68. 알림 삭제 — 읽은 것 일괄/단건 (`DELETE .../read`, `/{id}`) | 401 | O(단건은 본인만, 아니면 403) | 동일 | 동일 |

**첨부파일(Attachment)**

| **API/기능** | **GUEST** | **USER** | **MENTOR** | **ADMIN** |
| --- | --- | --- | --- | --- |
| F-69. 이미지 업로드 서명 발급 (`POST /api/attachments/images/signature`, `/profile-image/signature`) | 401 | O | O | O |
| F-70. 일반 파일 업로드 (`POST /api/attachments/files`) | 401 | O | O | O |
| F-71. 파일 다운로드 (`GET /api/attachments/files/{attachId}/download`) | 401 | O(업로드한 본인이거나, 공개 질문에 첨부된 파일인 경우) | 동일 | 동일 |

**정산(Settlement)**

| **API/기능** | **GUEST** | **USER** | **MENTOR** | **ADMIN** |
| --- | --- | --- | --- | --- |
| F-72. 정산 계좌 조회 (`GET /api/v1/settlement-accounts/me`) | 401 | O(역할 검증 없음, 등록 안했으면 404류) | O(본인) | O(역할 검증 없음) |
| F-73. 정산 계좌 등록 (`POST /api/v1/settlement-accounts/me`) | 401 | 403 | O(본인) | O |
| F-74. 정산 내역 조회 (`GET /api/v1/settlements/me`) | 401 | 403 | O(본인) | 403 |
| F-75. 출금 신청 (`POST /api/v1/settlements/me/request-withdrawal`) | 401 | 400(대기 중 정산 없음 — 역할 차단 아님) | O(대기 정산 있는 경우) | 400(동일) |

**관리자(Admin) 나머지 · 문의(Inquiry)**

| **API/기능** | **GUEST** | **USER** | **MENTOR** | **ADMIN** |
| --- | --- | --- | --- | --- |
| F-76. 전체 회원 목록 (`GET /api/admin/users`) | 401 | 403 | 403 | O |
| F-77. 회원 검색 (`GET /api/admin/users/search`) | 401 | 403 | 403 | O |
| F-78. 회원 차단/해제 (`PATCH /api/admin/users/{userId}/block`, `/unblock`) | 401 | 403 | 403 | O |
| F-79. 회원 강제 탈퇴 (`DELETE /api/admin/users/{userId}`) | 401 | 403 | 403 | O |
| F-80. 대기 환불 목록 (`GET /api/admin/cancellations`) | 401 | 403 | 403 | O |
| F-81. 환불 승인/거절 (`PATCH /api/admin/cancellations/{id}/approve`, `/reject`) | 401 | 403 | 403 | O |
| F-82. 정산 목록 조회 (`GET /api/admin/settlements`) | 401 | 403 | 403 | O |
| F-83. 정산 완료 처리 (`PATCH /api/admin/settlements/{id}/complete`) | 401 | 403 | 403 | O |
| F-84. 비회원 문의 등록 (`POST /api/v1/inquiries`) | O | O | O | O |
| F-85. 문의 목록 조회 (`GET /api/admin/inquiries`) | 401 | 403 | 403 | O |
| F-86. 문의 상태 변경 (`PATCH /api/admin/inquiries/{id}/status`) | 401 | 403 | 403 | O |

**웹훅(Webhook) · 헬스체크**

| **API/기능** | **GUEST** | **USER** | **MENTOR** | **ADMIN** |
| --- | --- | --- | --- | --- |
| F-87. PortOne 웹훅 수신 (`POST /api/v1/webhooks/portone`) | O(단, HMAC 서명 검증 실패 시 처리 거부 — 스프링 시큐리티가 아니라 애플리케이션 레벨 인증) | 해당 없음(PG사가 호출) | — | — |
| F-88. 헬스체크 (`GET /health`, `GET /`) | O | O | O | O |